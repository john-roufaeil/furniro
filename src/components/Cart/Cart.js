import React from 'react';
import PropTypes from 'prop-types';
import './Cart.css';

const Cart = () => (
  <div className="Cart">
    Cart Component
  </div>
);

Cart.propTypes = {};

Cart.defaultProps = {};

export default Cart;
